#include <stdlib.h>
#include <stdio.h>
#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
int c;

for (c = 0; c < 10; c++)
{
putchar((c % 10) + 0);
putchar('\n');
return (0);
}
